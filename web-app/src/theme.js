// src/theme.js
import { useState } from 'react'; // Import statements should be at the top

const theme = {
  light: {
    background: '#ffffff',
    color: '#000000',
  },
  dark: {
    background: '#000000',
    color: '#ffffff',
  },
};

export default theme;
